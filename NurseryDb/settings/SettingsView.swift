import SwiftUI

// Settings View
struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Settings Screen")
                .font(.largeTitle)
                .padding()
            // Add your content here
            Spacer()
        }
        .navigationTitle("Settings")
    }
}
