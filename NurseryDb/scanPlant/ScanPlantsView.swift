import SwiftUI

// Scan Plants View
struct ScanPlantsView: View {
    var body: some View {
        VStack {
            Text("Scan Plants Screen")
                .font(.largeTitle)
                .padding()
            // Add your content here
            Spacer()
        }
        .navigationTitle("Scan Plants")
    }
}
